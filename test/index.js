
'use strict';

// const expect = require('expect');

/**
 * mocha test --watch
 */

describe('mongoose-to-swagger', function () {
  describe('export', () => {
    beforeEach(() => { });
    it('should do something', () => ({}));
  });
});
